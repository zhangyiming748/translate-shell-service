version: '3.8'

services:
  gin:
    image: zhangyiming748/translate-service:latest
    ports:
      - "8192:80"
    container_name: gin
    volumes:
      - ./log:/log
      - ./data:/data
    environment:
      - abracadabra=abracadabra
      